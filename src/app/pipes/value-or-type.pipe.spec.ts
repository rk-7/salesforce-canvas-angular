import { ValueOrTypePipe } from './value-or-type.pipe';

describe('ValueOrTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueOrTypePipe();
    expect(pipe).toBeTruthy();
  });
});
